# 用户：
- `README.md`
- 我希望用 RAG 实现将题目和能帮助题目生成测试用例的相关文档合并发送给大模型。 请帮我修改README，将性能改进放到后面去做，先解决RAG。 计划： 1. 根据换行符、{}、或 indent 对代码分片，并用适当的大模型生成代码描述，再用较大模型生成各分片之间的依赖概率（可能有更专业的名称）。 2. 依赖概率：例如代码中 方法A 依赖 方法B 而 B完全可以独立与A使用，则可以设如： p(A,B) = 0.9 , P(B,A)=0.4 3. 依赖概率可以一开始给出，但是在后序实际使用中可以收集非隐私数据，例如某次提问向量如何，然后只给出文档 A 时，大模型回答错误，用户调整后 追加给出文档B，则 P(A,B) 需要调高。 4. 将来还要能够自动运行代码，并将代码执行结果反馈给RAG，以供下一次大模型使用。

# GPT5
`GPT5-1.md`

# 用户
如下是我之前基于专家知识写的prompt工程（比较粗糙）以供参考：`ai_prompts.py`
后面采用RAG，则该（上文）框架弃用，改为选择纳入RAG的代码、说明文档等，构建知识库，自适应合成提示词。 请设计第一版最小可运行架构（MVP）

# GPT5
`GPT5-2.md`

# 向DeepSeek提问
- 请帮我压缩整理 README 的下一步计划，要求精简，符合原本格式
1. 近期计划
   - 用XX模型
   - 如何切片
2. 将来计划
   - 进一步提高执行暴力算法的效率
   - 依赖概率矩阵...
   - 自动调用工具（如用复杂的 LangGraph）