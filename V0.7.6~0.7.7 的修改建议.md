问题其实已经暴露在你给出的两个文件里了，不是 `embedding.py`，也不是 `index_builder.py`，而是 **`semantic_chunker.py` 根本没有支持嵌套 RAG 模块**。

我按照你的源码走了一遍流程，问题发生的位置如下。

---

# 1. parse() 的实际执行过程

你的文件：

```python
# @RAG_BEGIN: case_generator_function

def case_generator(...):
    ...

# @RAG_END

    # @RAG_BEGIN: scale2n
    ...
    # @RAG_END
```

而 parser 是：

```python
m = self.RE_RAG_BEGIN.match(line)
if m:
    current_module = ...
    module_lines = []
    continue
```

进入以后：

```python
if current_module:
    module_lines.append(line)

    m_end = self.RE_RAG_END.match(line)
    if m_end:
        ...
        current_module = None
```

注意这里只有

```
current_module
```

一个变量。

没有 stack。

也就是说：

```
BEGIN A
    BEGIN B
    END
END
```

这种情况它根本不会处理。

---

# 2. 更严重的是你的布局

你的代码实际上是

```
BEGIN case_generator_function

def ...

END

BEGIN scale2n

END

BEGIN distinct_strategy

    BEGIN nums_range_strategy

    END

END
```

注意：

```
case_generator_function
```

在第一个

```
@RAG_END
```

这里已经结束了。

因此：

```
embedding_text
```

只有

```
def case_generator(...):
    """
    ...
    """
```

没有任何函数体。

所以生成的

```
.semantic.json
```

只有

```
module_count = 1
modules=["case_generator_function"]
```

完全符合 parser 的行为。

不是 Chroma 的问题。

---

# 3. 为什么没有 scale2n ?

真正的问题来了。

理论上，

后面的

```
BEGIN scale2n

END
```

应该还能解析。

但是你的 json

```
module_count = 1
```

说明后面的 BEGIN 根本没进入。

这里只有两种可能。

---

## 第一种（概率最高）

你贴的源码不是实际运行源码。

因为按 parser：

```
current_module=None
```

以后，

继续 for，

再次遇到

```
BEGIN scale2n
```

一定会：

```
current_module="scale2n"
```

最后

```
rag_modules["scale2n"]
```

一定存在。

绝不会只有1个 module。

---

## 第二种

你的真实源码缩进导致 RE 没匹配。

例如：

```
    # @RAG_BEGIN: scale2n
```

regex 是

```python
RE_RAG_BEGIN = re.compile(
    r"#\s*@RAG_BEGIN:\s*(\w+)"
)
```

它要求：

```
line
```

开头就是

```
#
```

但是你的实际 line 是

```
"    # @RAG_BEGIN..."
```

前面四个空格。

于是：

```
match()
```

失败。

注意这里是

```
match()
```

不是

```
search()
```

因此：

```
    # @RAG_BEGIN
```

不会匹配。

这就是为什么：

```
case_generator_function
```

可以匹配（因为它没有缩进）

而

```
scale2n
```

完全消失。

---

## 这也是我认为真正的问题。

你的正则：

```python
r"#\s*@RAG_BEGIN:"
```

应该改成：

```python
r"\s*#\s*@RAG_BEGIN:"
```

其它所有 marker 都一样。

例如

```
RE_RAG_END
RE_RAG_DEP
RE_RAG_EXPORT
...
```

全部需要允许前导空格。

例如：

```python
RE_RAG_BEGIN = re.compile(
    r"\s*#\s*@RAG_BEGIN:\s*(\w+)"
)

RE_RAG_END = re.compile(
    r"\s*#\s*@RAG_END"
)

RE_RAG_EXPORT = re.compile(
    r"\s*#\s*@RAG_EXPORT:\s*(\w+)"
)
```

否则函数体里的 marker 永远识别不到。

---

# 4. 即使修完，还有一个设计问题

假设改成允许缩进。

现在会得到：

```
case_generator_function
scale2n
distinct_strategy
nums_range_strategy
nums_shuffle_strategy
...
```

但是：

```
case_generator_function
```

依旧只有：

```
def case_generator(...):
```

原因就是：

```
@RAG_END
```

放在函数声明后。

parser 会认为：

```
module 到这里结束。
```

所以函数体永远不会属于它。

---

# 5. 你真正想实现的是另一种语义

看你的代码，我认为你的设计其实是：

```
case_generator_function
│
├── scale2n
├── distinct_strategy
│      ├── nums_range_strategy
│      └── nums_shuffle_strategy
├── sorted_partial
└── return_nums
```

也就是说：

整个函数是一个模块。

里面还能继续切子模块。

这是**层次模块（Hierarchical Module）**。

但你当前 parser 是：

```
BEGIN
...
END
```

线性 parser。

它不支持：

```
BEGIN
    BEGIN
    END
END
```

因此：

你的 RAG 标记格式和 parser 的能力是不一致的。

---

# 我的建议（推荐程度）

按长期来看，我建议不要仅仅修改正则，而是升级 `SemanticChunker`：

1. **第一步（必须修复）**：所有正则允许前导空格（`\s*#`），否则函数内部标记全部失效。
2. **第二步（推荐）**：将 `current_module` 改为 **模块栈（stack）**，支持嵌套 `@RAG_BEGIN/@RAG_END`，这样才能正确解析你设计的层次结构。
3. **第三步（推荐）**：对子模块建立 `parent` 关系，后续 Graph-RAG 可以直接利用，而无需重新设计数据结构。

**结论：** 目前的问题不是 ChromaDB、Embedding 或 Index Builder，而是 `SemanticChunker` 的解析器能力与标记语法不匹配。最直接导致 `scale2n` 等模块丢失的是 `re.match()` 配合没有 `\s*` 的正则；而导致 `case_generator_function` 只有函数声明、没有函数体的是当前解析器采用线性 `BEGIN/END` 模型，不支持你实际使用的嵌套模块设计。
